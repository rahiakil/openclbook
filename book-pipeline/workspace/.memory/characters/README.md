# Character memory (hidden from main manuscript)

Put one file per character or setting bible, for example `hero.md`, `antagonist.md`.
The agent always receives concatenated Markdown from everything under `.memory/`.
